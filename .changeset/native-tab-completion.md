---
"@alloc/cmd-ts": minor
---

Add first-class `@bomb.sh/tab` shell completion support to commands by default, including semantic completion for nested subcommands, aliases, flags, options, positional arguments, and custom value suggestions.
